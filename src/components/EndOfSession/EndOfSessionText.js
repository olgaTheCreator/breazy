import React from "react";
import "../BeginSessionText/beginSessionText.css";

export const EndOfSessionText = () => {
  return (
    <div className="begin-session">Your breathing session has finished.</div>
  );
};
